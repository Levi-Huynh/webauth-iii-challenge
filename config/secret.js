

module.exports = {
    jwtSecret: process.env.JWT_SECRET || 'Shh this is the secret here!' ,
    mantra: 'for many to many, third table',
    eitherMantra: 'one to many > foreign key',
    yetAnotherMantra: 'where does the FK go..the many side'
}